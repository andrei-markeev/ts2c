var int_arr = [10, 20];
int_arr.pop();
int_arr.push(100);
int_arr.push(200);
int_arr.push(300);
console.log(int_arr);

int_arr.pop();
int_arr.pop();
int_arr.pop();
int_arr.pop();
int_arr.pop();
int_arr.pop();
console.log(int_arr);
